(setopt x #true)

;; (is-set? i)
;; (get i)
;; ;; i

;; (define i 0)

;; i
;; (is-set? i)
;; (get i)

;; (define test (lambda () 1))
;; (test)
;; (define (test a) (+ 1 a))
;; (test 2)

(define make-adder (lambda (x) (lambda (y) (+ x y))))
(define add5 (make-adder 5))
(set make-adder #n) ;; The original 'make-adder' environment could be freed here
(add5 10) ;; Will this crash?
